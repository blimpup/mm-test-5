# An open pull request whose plan is held open by a hook, so that the test can push to
# the feature branch while the plan runs (RFD 2356).

engine:
  name: tofu

hooks:
  plan:
    pre:
    - cmd:
      - 'sleep'
      - '60'
      type: run
