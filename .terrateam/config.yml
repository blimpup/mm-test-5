# An open pull request whose apply is held open by a hook, so that the test can push to
# the feature branch while the apply runs (RFD 2356).

engine:
  name: tofu

hooks:
  apply:
    pre:
    - cmd:
      - 'sleep'
      - '60'
      type: run
