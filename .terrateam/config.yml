# A plan of a merged pull request, asked for by a comment and held open by a hook, so
# that the test can move the destination branch while it runs (RFD 2356).

engine:
  name: tofu

hooks:
  plan:
    pre:
    - cmd:
      - 'sleep'
      - '60'
      type: run
